<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                      http://maven.apache.org/xsd/settings-1.0.0.xsd">

  <servers>
    <!-- Solo lectura, para resolver spring-boot-starter-cork como dependencia. Mismo
         mecanismo que usan hoy los demas repos del org (segment-api, dahua-api, etc). -->
    <server>
      <id>cork</id>
      <username>fmonsalvo</username>
      <password>a97885612083565d579c28fce1508ca9246c6235</password>
    </server>
    <!-- Publicacion del propio paquete: credenciales via env vars del workflow, no
         hardcodeadas (ver publish.yml). -->
    <server>
      <id>platori</id>
      <username>${env.GITHUB_ACTOR}</username>
      <password>${env.GITHUB_TOKEN}</password>
    </server>
  </servers>
</settings>
